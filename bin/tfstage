#!/usr/bin/env python

import argparse
import tfstage

parser = argparse.ArgumentParser(description='TensorFlow scaffolding generator.')
parser.add_argument('name', type=str, help='Project name')

args = parser.parse_args()

tfstage.generate_project(args.name)
